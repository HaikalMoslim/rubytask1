<div>
<h1><%=@post.title%></h1>
<p><%=@post.content%></p>

<ul>
    <li><%= link_to "Back", posts_path %></li>
    <li><%= link_to "Edit", edit_post_path(@post) if user_signed_in?%></li>
</ul>

<h2 class="font-bold text-4xl">Comments</h2>
<%= render @post.comments%>

<h2>Add a comment</h2>
<%= render 'comments/form'%>
</div>